<EditorForm TContent="FestaViewModel" Model="Model" OnSave="SubmitAsync" OnCancel="CancelAsync">
    <ContentInfo>
        <MudGrid>
            <MudItem xs="12" md="3">
                <MudDatePicker Label="Data Inizio" @bind-Date="Model.DataInizio" For="@(() => Model.DataInizio)" Variant="Variant.Outlined" Editable="true" AutoClose="true" id="datainizio" />
            </MudItem>
            <MudItem xs="12" md="3">
                <MudDatePicker Label="Data Fine" @bind-Date="Model.DataFine" For="@(() => Model.DataFine)" Variant="Variant.Outlined" Editable="true" AutoClose="true" id="datafine" />
            </MudItem>
            <MudItem xs="12" md="3">
                <MudSelect Label="Stato Festa" @bind-Value="EnumValue" Variant="Variant.Outlined" ReadOnly="true" id="status">
                    @foreach (FestaStatus item in Enum.GetValues(typeof(FestaStatus)))
                    {
                        <MudSelectItem Value="@item">@item</MudSelectItem>
                    }
                </MudSelect>
            </MudItem>
        </MudGrid>
    </ContentInfo>
</EditorForm>